name: GitHub Readme YouTube Cards
on:
  schedule:
    # Runs every hour, on the hour
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: ikx7a/github-readme-youtube-cards@main
        with:
          channel_id: UC9o1hM49jVr2lgOinw0pAdw
          background_color: "#07102B"
